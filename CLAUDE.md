# CLAUDE.md

This project uses **`AGENTS.md`** as the single source of truth for agent + contributor instructions, so that Claude Code, Codex, and other agents all follow the same rules.

**Read `./AGENTS.md` first, then `./plan/README.md`.**

Anything Claude-Code-specific can go below — but keep all *shared* rules in `AGENTS.md` so other agents stay in sync. (Don't duplicate rules here.)
